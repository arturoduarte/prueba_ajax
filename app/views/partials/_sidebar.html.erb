<nav class="fixed-top navbar navbar-expand-lg navbar-light white-text bg-success sombra">
	
	<% if user_signed_in? %>
	<a class="navbar-brand" href= <%= root_path %> >Inicio</a>
	<% end %>
	
	<button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
		<span class="navbar-toggler-icon"></span>
		
	</button>
	
	<div class="collapse navbar-collapse" id="navbarSupportedContent">
		<% if user_signed_in? %>
		<ul class="navbar-nav">
			<li class="nav-item">
				<!-- <a class="nav-link bold" href= "#" > Historial</a> -->
			</li>
		</ul>
		
		<% end %>
		
		
		<div class="dropdown-divider"></div>
		<ul class="navbar-nav ml-auto">
		
			<% if user_signed_in? %>
			<li class="nav-link bold">
				Usuario: <%= current_user.email %>
			</li>
			
			<li class="nav-item">
				<%= link_to 'Cerrar sesión', destroy_user_session_path, method: :delete, class: 'nav-link' %>
			</li>
			
			<% else %>
			<li class="nav-item">
				<%= link_to 'Iniciar Sesión', new_user_session_path, class: 'nav-link' %>
			</li>
			
			<li class="nav-item">
				<%= link_to 'Registrarse', new_user_registration_path, class: 'nav-link' %>
			</li>
			<% end %>
		</ul>
		
		
	</div>
</nav>